# Testing

A simple HTTP server built with Node.js.

## Prerequisites

- [Node.js](https://nodejs.org/)

## Usage

Start the server:

```bash
node server.js
```

The server will start on [http://localhost:3000](http://localhost:3000) and respond with `Hello, World!` to all requests.
